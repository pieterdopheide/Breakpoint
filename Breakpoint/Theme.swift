import Foundation
import SwiftUICore

extension Color {
    static let flow = Color(red: 193 / 255, green: 92 / 255, blue: 92 / 255)
    static let breakpoint = Color(red: 57 / 255, green: 112 / 255, blue: 151 / 255)
}
